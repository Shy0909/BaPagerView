# 手动验收清单

在 `BaPagerViewExample` 中分别打开 Swift 和 Objective-C 示例页，检查：

- 初始加载后显示五张卡片，拖动后吸附到当前对齐位置。
- 切换左、中、右对齐，观察卡片位置与缩放进度。
- 切换自动轮播方向、间隔和动画时长，确认计时滚动正常。
- 开关无限循环，并用“重新加载”切换两张与五张数据，确认索引和复用正常。
- 离开页面再返回，确认自动轮播不会在后台页面继续运行。

此目录目前是人工验收说明，尚无 XCTest 自动化用例。
